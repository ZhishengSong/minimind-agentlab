run_name: sft_minimind_webnav_smoke
base_checkpoint: outputs/tooluse_init/pretrain_step_050000_tooluse_init.pt
tokenizer_path: outputs/tooluse_init/tokenizer
train_data_path: outputs/minimind_sft/sft_train_next_action.jsonl
eval_data_path: outputs/minimind_sft/sft_eval_next_action.jsonl
output_dir: checkpoints/sft_minimind_webnav_smoke
log_dir: logs

seed: 42
device: auto
dtype: bf16

max_seq_len: 1024
batch_size: 2
gradient_accumulation_steps: 4
max_steps: 20

learning_rate: 5.0e-5
min_learning_rate: 5.0e-6
weight_decay: 0.01
warmup_steps: 2
max_grad_norm: 1.0

log_interval: 1
save_interval: 10
num_workers: 0
